import { SingupForm } from "@/features/auth/components/singup-form";

export default function page() {
    return (
        <main className="flex items-center justify-center min-h-screen w-full max-sm:px-5">
            <SingupForm />
        </main>
    )
}
